# Samples coupling file.
# This file is intermediate between the root and sample's CMake files. Separate
# responsibility of each directory configuration for easier manipulation.

# Declare samples.
add_subdirectory(print_aswell)
add_subdirectory(print_something)
