# Modules coupling file.
# This file is intermediate between the root and module's CMake files. Separate
# responsibility of each directory configuration for easier manipulation.

# Declare modules.
# add_subdirectory()
