services:
  redis:
    image: "redis:alpine"
  one:
    build: ./one
    ports:
      - "3000:3000"
    depends_on:
      - redis
  two:
    build: ./two
    ports:
      - "4000:4000"
    depends_on:
      - one
